type Query {
    bookById: Book
}

type Mutation {
    addBook(book: InputBook): Book
}

type Book {
    id: Int
    name: String
}

input InputBook {
    name: String
}

type Author {
    id: ID
    firstName: String
    lastName: String
}